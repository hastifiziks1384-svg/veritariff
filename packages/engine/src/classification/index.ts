// HS classification verification (§5.4): deterministic legal chain —
// facts → Section/Chapter notes (hard exclusions) → heading (GIR 1–5) →
// HS-6 (GIR 6). Reference data (headings, notes) is passed IN from the
// tariff client; this module performs no I/O.
export * from "./rules";
export * from "./classify";
