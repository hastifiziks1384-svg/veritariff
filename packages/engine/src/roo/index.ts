// Rules of origin, Levels 1–2 (Phase 5): looks up the applicable TCA
// product-specific rule for a heading from the curated Ch 72–73 table in
// data/roo-rules. Verification/explanation only — this module must never
// assert that goods qualify.
export const ROO_ENGINE_PLACEHOLDER = "phase-5" as const;
